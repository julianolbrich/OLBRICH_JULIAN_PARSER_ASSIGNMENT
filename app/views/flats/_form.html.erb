<%= simple_form_for(@flat) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :address, label: false, placeholder: "Please select an address from the autocomplete dropdown while searching .." %>
  </div>

  <p> <em> tips: use '..straße' instead of '..str.' </em> </p>

  <div class="form-actions text-left" style="margin-bottom: 10px;">
    <%= f.button :submit, value: "PARSE ADDRESS", class: "btn btn-primary", method: :post, remote: true %>
  </div>

  <br>
<% end %>
