<%= simple_form_for(@flat) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :address, autofocus: true, label: false, placeholder: "While searching please select your address from the listed choices .." %>
  </div>

  <p> <em> tips: use '..straße' instead of '..str.' </em> </p>

  <div class="form-actions text-left" style="margin-bottom: 0px;" id="parse-button">
    <%= f.button :submit, value: "PARSE ADDRESS", class: "btn btn-primary", method: :post, remote: true %>
  </div>

  <br>
<% end %>
